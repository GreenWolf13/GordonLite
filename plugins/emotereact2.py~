from util import hook
import random
import re

emotes2 = ["pets"]
reactions2 = [":3"]

@hook.regex("ACTION.*(" + "|".join(emotes2) + ") (?:the )?(\S+)\x01", re.I)
def react(inp, input=None):
    if inp.groups()[1].lower() == input.conn.nick.lower():
        input.say(random.choice(reactions2))
